# The directory with basic components such as button, inouts etc.

This is legacy components library which used for building questions in builder. Now components are overwriting from ui primitives. But options of rendering now is limited. 